#include <iostream>
#include <vector>
#include <cstring>
using namespace std;
int main() {
  int a = 0;
  while (a==0) {
    char str[81];
    
    cin>> str;
    if (strcmp(str,"quit") == 0) {
      a = 1;
    }
  }
  return 0;
}
